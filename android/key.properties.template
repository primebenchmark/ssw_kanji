# Copy this file to android/key.properties and fill in your production keystore details.
# NEVER commit android/key.properties — it is already in .gitignore.
# In CI, inject these values as environment variables or GitHub Actions secrets.

storeFile=../keystore/release.jks
storePassword=REPLACE_WITH_STORE_PASSWORD
keyAlias=REPLACE_WITH_KEY_ALIAS
keyPassword=REPLACE_WITH_KEY_PASSWORD
