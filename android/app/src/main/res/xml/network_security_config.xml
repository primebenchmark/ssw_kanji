<?xml version="1.0" encoding="utf-8"?>
<!--
  Android Network Security Configuration
  - Blocks all cleartext (HTTP) traffic app-wide
  - Trusts only the system CA store (no user-installed CAs in release)
  - Debug builds additionally trust user-installed CAs to allow proxy tools
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
